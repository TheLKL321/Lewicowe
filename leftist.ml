type a' queue =
  | Node of a' queue * a' * a' queue
  | Null

let empty = Null

let add a q =

exception Empty;;

let delete_min =
  |

  | Null -> Empty

let join q1 q2 =

let is_empty q =
  if q = Null then
    true
  else
    false
;;
